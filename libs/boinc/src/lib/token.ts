import { InjectionToken } from '@angular/core';
export const BOINCSERVER = new InjectionToken<string>(
  'Boinc server url'
);

export const TOTALCREDITMATHS = new InjectionToken<string>(
  'Boinc credit Maths'
);
